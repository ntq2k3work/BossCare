# Admin Overview Exec Plan

## Steps

1. Add store-level stats contracts for auth, pets, and payments.
2. Add admin stats service and `/api/admin/overview`.
3. Build `/admin` overview page with core totals and payment review shortcut.
4. Add story packet, matrix row, verify script, and full verification evidence.

## Rollback

Revert admin stats contracts, `/api/admin/overview`, `/admin`, admin nav/copy, E2E test, package script, and story/matrix docs.
